{{#partial "contenido"}}
<div class= "row justify-content-center">
      <div class="col-4">
        <h1 class="text-info text-center">Mis Indicadores</h1>
        <table class="table table-striped" id="indicadores">
          <thead class="table-inverse">
            <tr>
              <th>#</th>
              <th class="text-center">Nombre</th>
              <th class="text-center">Fórmula</th>
              <th class="text-center"></th>
            </tr>
          </thead>
          <tbody class="empresas_list">
          {{#each this}}
          <tr>
            <th scope="row">{{@index}}</th>
            <th class="text-center">{{nombre}}</th>
            <th class="text-center">{{formula}}</th>
          </tr>
          {{/each}}
          </tbody>
        </table>
      </div>
   </div>

  <div class="row justify-content-center"">
  <h4 class="text-info text-center">Eliminar Indicador</h4>
  </div>
  <div class="row justify-content-center"">
  <div class="col-4">  
    <form action="/indicadores/delete" method="post">
     <div class="form-group">
       <label for="indicador">Elegir Indicador</label>
        <select name="indicador">
          <option value="null" selected="selected"></option>
          {{#each this}}
           <option value="{{nombre}}" {{#selected}} selected="selected"{{/selected}}> {{nombre}} </option>
          {{/each}}
        </select>
      <button class="btn btn-primary" type="submit">Eliminar</button>
     </div> 

      </form>
   </div>
 </div>

 {{/partial}}
{{>userspace.hbs}}
